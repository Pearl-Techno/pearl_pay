import 'package:flutter/material.dart';

class P10Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('P10 Screen'),
      ),
      body: Center(
        child: Text('Welcome to P10 Screen!'),
      ),
    );
  }
}