import 'package:flutter/material.dart';

class P9Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('P9 Screen'),
      ),
      body: Center(
        child: Text('Welcome to P9 Screen!'),
      ),
    );
  }
}