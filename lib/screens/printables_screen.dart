import 'package:flutter/material.dart';

class PrintablesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Printables'),
      ),
      body: Center(
        child: Text('Printables Screen Content'),
      ),
    );
  }
}